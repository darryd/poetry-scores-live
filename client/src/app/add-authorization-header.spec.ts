import { AddAuthorizationHeader } from './add-authorization-header';

describe('AddAuthorizationHeader', () => {
  it('should create an instance', () => {
    expect(new AddAuthorizationHeader()).toBeTruthy();
  });
});
