import { MaintainConnection } from './maintain-connection';

describe('MaintainConnection', () => {
  it('should create an instance', () => {
    expect(new MaintainConnection()).toBeTruthy();
  });
});
